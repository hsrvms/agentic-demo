meta {
  name: Get Invoice — Invalid ID
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/invoices/not-a-uuid
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  X-Tenant-ID: {{tenant_id}}
}

script:post-response {
  test("Status code is 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
meta {
  name: Delete Schedule
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/api/schedules/{{weekly_schedule_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
  X-Tenant-ID: {{tenant_id}}
}

script:post-response {
  test("Status code is 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}